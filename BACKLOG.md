---
state: active
next: Nothing blocking. To publish a week: run the research prompt in `prompts/weekly-research.md` against `config/brief.yml`, save the result as `data/<monday>.json`, then in this repo run `python3 scripts/validate.py && python3 scripts/build.py` and commit — see [CLAUDE.md](CLAUDE.md) for the pipeline. Specs go in a root-level `specs/`, never `docs/` (the Pages web root); each declares its own tier per `delivery-tiers`.
tool: claude-code
updated: 2026-08-23
---

# Backlog — culture-vulture

## Utility now
<!-- utility-updated: 2026-08-22 -->
- Live site at https://alexistosteson.github.io/culture-vulture/ serves `docs/index.html` against `docs/events.json`, currently showing the window 2026-08-17 to 2026-08-23 with real Bay Area listings across five ranked regions (south-bay, peninsula, sf, east-bay, outer) and real venues (Excite Ballpark, PayPal Park, Mountain Winery, PURE Nightclub, Shoreline Amphitheatre, and others).
- The v0.5 Almanac redesign shipped 2026-08-15 (`97028e6`, tagged `v0.5.0`) and is the front end currently live.
- `scripts/validate.py` and the tooling floor (`ruff.toml`, `python3 ~/.claude/bin/tool-floor.py`) run in CI on every push — the floor established 2026-08-15 (`d3c86da`) and both halves enforced in CI 2026-08-17 (`2828223`, `07afd2d`).
- `scripts/verify.sh` mechanically gates merges to `main` (`732d667`/`4ddbc5b`, "let verified work merge itself"), and weekly runs have merged through it since, e.g. "Week of 2026-08-17: 88 events" (`03d5a6d`/`cfee013`).

Weekly Bay Area arts digest. One HTML file, one JSON file, no framework or
backend. Live at https://alexistosteson.github.io/culture-vulture/ · released
`v0.5.0` 2026-08-15 (the Almanac redesign; `v0.1.0` same day).

**Deliberately light on process, not unfinished.** It arrived as a downloaded
tarball with its own git history rather than through `new-swe-project`, so it
has no CONSTITUTION and no ritual docs — and per `delivery-tiers` it does not
owe them, because tier is declared per spec and most work here is `probe`. The
one gate that never relaxes, the tooling floor, is established (`ruff.toml`,
2026-08-15) and enforced in CI (2026-08-17). This file plus
[CLAUDE.md](CLAUDE.md) are the whole process surface.

## Index

- medium · **Unattended weekly publish sits unmerged on `spec/weekly-autopublish`** — a disabled routine, three falsified provisioning assumptions, and an open push-to-`main` question that exist nowhere on this branch
- small · Design port package is two designs stale — now snapshots a front end that no longer exists
- small · Full build-tier scaffold, only if a spec ever needs it — parked, see the row below for why it is not owed now
- one-liner · Two rough edges in the v0.5 `¶` overlay — both seen in the shipped page
- small · Copy-link buttons on the event drawer and the Plan panel — the drawer needs a deep link before it can have one
- medium · Venue / ticket link on the event drawer — the schema already allows `url`; nothing produces it
- medium · Repoint the site at another metro as a probe — Traverse City / Olympia / San Juan; tests the project's central claim
- large · Spanish-language edition — three separable layers, only one of which is the page

---

| Item | Context | Do it when |
|---|---|---|
| **Write the `specs/`-not-`docs/` layout constraint into `CLAUDE.md`.** Pages serves this repo from `main` + `/docs`, so anything written to `docs/HANDOFF.md`, `docs/handoff/` or `docs/superpowers/{specs,plans}` is publicly served — short-form probe specs therefore go in a root-level `specs/`. Also record that the template must not be copied piecemeal (its `CLAUDE.md` links a tree that would not exist). Then strike the parked-scaffold row. [feature/s2/v3] | Amended 2026-08-23 (Step 4, owner accepted the proposed rewrite). It replaced the parked *Full build-tier scaffold* record-row: the scaffold itself is genuinely not owed until a spec here is declared **build** tier, but the layout constraint inside it is live and was written nowhere a future session would read. The superseded analysis — the three scaffold options, the `docs/_config.yml` and `gh-pages` alternatives, and the project-scope probe declaration that does not apply — survives in this file's git history. | Now. The constraint is live; the scaffold it came from is still not owed. |
| Unattended weekly publish — unmerged on `spec/weekly-autopublish` [blocker/s3/v3] → preserves the only copy of the autopublish provisioning findings before the routine is enabled or the worktree is deleted | **This row exists because the work was invisible from `main`.** The 2026-08-21 stale-backlog audit found the whole autopublish effort recorded only in the worktree `../bay-week-autopublish` (branch `spec/weekly-autopublish`), which the portfolio scanner counted as its own workstream; `main`'s backlog mentioned none of it.<br><br>**State as of 2026-08-16:** a second routine `trig_01U3f8NYCvzd7WEbYSNEbvNK` ("CultureVulture 2", cron `0 3 * * 1` UTC = Sunday 20:00 PDT) exists and is **disabled — it must stay disabled until the probe passes**. `PUSH_TARGET` is `main`, production, **no review gate**. Research egress is fixed; the site has never been published to by a routine and the push to `main` has never been attempted. **Three assumptions in `handoff/cloud-provisioning.md` are false** — detail in the branch's `handoff/weekly-autopublish.md`, which is the authoritative record and is not on `main`.<br><br>The branch also carries its own open row — *the weekly run is automated and publishes unreviewed, and the owner's two-phase check is outstanding*. `spec/sources-expansion` is a second unmerged worktree of this repo. | Before enabling the routine, and before anyone deletes either worktree — the branch is the only copy of the provisioning findings. |
| ~~Design port package is two designs stale [bug/S]~~ | `handoff/claude-design/` snapshots the site **before** the Culture Vulture rename, before the outdoor-chip fix, and now before the whole v0.5 Almanac redesign (`97028e6`) — its `source/index.html` is a front end the project no longer has, its README cites commit `a1e7b0f`, and all five screenshots show the old masthead, the old Barlow/Inter type and the viridis ramp. **Regenerating it is no longer a re-screenshot — the package's own brief is superseded**, and the design it was built to hand off has been replaced by the Almanac handoff in `Website design exploration.zip`. A chip was spawned 2026-08-15 against the smaller version of this problem. The capture method still holds: `--window-size` alone silently produces desktop-layout images at mobile pixel widths; load the page in a fixed-width `<iframe>` and screenshot the wrapper. | Before handing the package to anyone. Decide first whether it is a port package at all now, or just archived. |
| Copy-link buttons — event drawer and Plan panel [feature/s1/v2] | **The Plan half is nearly free; the drawer half is not.** The Plan panel already computes a shareable link and puts it in a readonly input at [index.html:449](docs/index.html:449) (filled from `location.href` at [index.html:818](docs/index.html:818)) — the ask is a button beside it that writes that string to the clipboard, `navigator.clipboard.writeText`, which needs a secure context: fine on Pages (HTTPS) and on `localhost`, so local verification works. Needs a confirmation state ("copied") and a fallback for a rejected clipboard permission — leave the input in place rather than replacing it, since select-and-copy is the fallback.<br><br>**The event drawer has nothing to share yet.** The hash carries filter state only — `axis`, the six `SETS`, `q` ([index.html:588](docs/index.html:588)) — and opening a drawer does not touch it, so there is no URL that reopens a given event. A copy button there owes first: a hash param (`e=<event id>`, ids are already stable and slug-safe), a write on open and a clear on close, and a `readHash` branch that reopens the drawer on load. Note the drawer is populated from the current `events.json`, so a shared event link goes stale the week after — decide whether that is acceptable or whether the link should degrade to the filtered week. | Together, as one spec — they share the copy helper and the confirmation state. The drawer's deep link is the real work and is worth having on its own. |
| Venue / ticket link on the event drawer [feature/s3/v2] | Currently the drawer's only outbound link is `Route in maps →` ([index.html:435](docs/index.html:435)). The ask: link to the venue's own page for *that* event where one exists, else the venue's top-level domain. **`schema/events.schema.json` already declares an optional `url` (`format: uri`) on an event and forbids unknown keys, so no schema change is needed** — but zero events in `data/` carry one, `prompts/weekly-research.md` never asks for it, and `index.html` ignores it. So the work is three-part and mostly not front end: extend the research prompt to capture a per-event URL, decide the precedence rule (event page > venue domain) and where the fallback domain comes from — a per-venue field in `brief.yml` beside the surveyed coordinates is the obvious home, since venues recur week to week and an event-level fallback would be re-researched every week — then render it in the drawer with `rel="noopener"`. Consider whether `validate.py` should warn on an event with no reachable link, and note that LLM-produced URLs are a hallucination surface: an unreachable link is worse than no link, so the validator earning a liveness check is part of the call. | Before the next week is researched, if at all — the prompt change is what makes the data start arriving, and retrofitting URLs onto past weeks by hand is not worth it. |
| Repoint at another metro — a portability probe [feature/s3/v2] | **This is the test of the project's central claim** ("repointing the site at another city is a config edit plus a rebuild"), so run it as a `probe` spec against a real second city and record where the claim breaks. Candidates named: Traverse City MI, Olympia WA, San Juan — *which* San Juan needs settling before research (Puerto Rico, the San Juan Islands, and San Juan Bautista CA are three different probes; PR also drags in the Spanish row below). Pick one; the point is one honest end-to-end run, not three.<br><br>**What is genuinely config, and will port:** `origin`, `regions` (names, order, ranks, colours), the vocabularies, and everything `build.py` projects into `docs/events.json`. **What will not, and is the actual finding to measure:** (1) `travel.venues` in `brief.yml` is a hand-authored table of ~30 Bay venues with lat/lng, parking and transit lines, and `travel.corridors` is hand-tuned bounding boxes with freeway names and tolls — a new metro starts with both empty, so every drawer falls to the "not surveyed" / "no coordinates" paths. Those fallbacks exist and are honestly worded, which is the good news; the probe should confirm the page is still *useful* in that state, because that is what any fork sees on day one. (2) `VENUE_MAP` and `VENUE_NOISE` at [index.html:605](docs/index.html:605) are hardcoded Bay venue abbreviations and a noise regex living in the page, not in config — a fork gets no abbreviations and possibly mangled labels. (3) Hardcoded Bay copy in the chrome: the meta description ([index.html:8](docs/index.html:8)) and `across the bay →` ([index.html:357](docs/index.html:357)). (4) The travel model is car-first from a single origin — plausible for Traverse City, wrong-shaped for a walkable or island market. (5) Volume: a small market may yield 15 events, not the Bay's 67, and the distance-band regions may collapse to two — worth seeing whether the layout and the region ramp survive a thin week. | Worth doing before any fork or hand-off, and before more work hardens Bay assumptions. Ship the finding as a written probe result even if nothing gets fixed — knowing which of the five actually bites is the deliverable. |
| Spanish-language edition [feature/s4/v1] | **Three separable layers, and only one is the page** — decide which are in scope before anything is built, because they have very different costs. (1) **Chrome**: the UI strings are inline in `index.html`, `lang="en"` is hardcoded at [index.html:2](docs/index.html:2), and day/month names are hardcoded English arrays at [index.html:456](docs/index.html:456) with no `Intl` use anywhere — so dates need `Intl.DateTimeFormat` (or parallel arrays) before any translation is honest. (2) **Config-supplied labels**: region names and the vocabularies already come from `brief.yml`, so they are translatable without touching the page — that part is nearly free and is an argument for putting the whole string table in config rather than in the HTML. (3) **Data**: event titles and `note` prose are English, written by the research prompt, and are the bulk of the words on the page. Translating them means either a bilingual field in the schema (`note_es`, which `additionalProperties: false` means is a real schema change) or a second research pass per week — a recurring editorial cost, not a one-time build. Also unresolved: one page with a toggle vs. a second built page, and whether the derived travel prose ("Leave by … to arrive 15 min early", the corridor warnings, assembled in JS) is worth translating or should be regenerated from a template table. | Not before the portability probe above — the same seam (strings and labels out of the page, into config) is what both rows want, and doing the probe first tells you whether to build for one locale or for N. If the target is a Spanish-speaking market rather than a Spanish view of the Bay, the two rows are one spec. |
| Two rough edges in the v0.5 `¶` overlay [bug/s1/v1] | Both observed in the shipped page 2026-08-15, neither serious enough to hold the release. **(a)** Adjacent selected region rows merge into one tinted block — the `#F1E8D6` wash swallows the `#EBE3D3` hairline between them, so two regions on read as one item. A darker `border-bottom` on `.region-row[data-on="true"]` separates them. **(b)** With the Venue block included, `CLEAR ALL` / `SHOW n` sit below the fold on a short laptop, so the primary action of the overlay is off-screen at rest. The block order is specified in the design handoff, so shortening it is a design decision, not a fix. | Next time the overlay is opened for any other reason. (a) is a two-line CSS change; (b) needs a call first. |
